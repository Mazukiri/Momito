# Archive

This directory preserves legacy applications that are no longer part of the active Momito build.

- `backend/` is the old Python backend. The backend of record is `apps/api`.
- `mobile/` is the old Expo mobile app. The active client is `apps/web`.

These trees are moved, not deleted, so history remains available through Git. Do not wire archived apps back into workspace scripts without a new explicit task.